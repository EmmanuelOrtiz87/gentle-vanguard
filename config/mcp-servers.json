{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "description": "File system access for foundation"
    }
  },
  "version": "1.0.0",
  "description": "MCP Server Configuration for foundation",
  "autoStart": true
}
