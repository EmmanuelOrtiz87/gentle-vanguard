Run the project test suite.

1. `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/validation/run-tests.ps1` — full suite
2. Report pass/fail counts
3. If failures: identify which tests failed and summarize errors
