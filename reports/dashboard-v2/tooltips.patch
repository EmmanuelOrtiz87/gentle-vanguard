--- a/app.js
+++ b/app.js
@@ -441,28 +441,28 @@
     
     // Live
     ui.renderSection('live', [
-      ui.card(t('cards.liveStatus'), '<span style="color:#37b8a8">● LIVE</span>', t('meta.eventStream')),
-      ui.card(t('cards.tokens'), data.tokens.used.toLocaleString(), t('meta.currentUsage')),
-      ui.card(t('cards.trafficLight'), `<span style="color:${statusColor}">${data.health.status}</span>`, t('meta.executiveStatus')),
-      ui.card(t('cards.routing'), data.health.routing, t('meta.dispatchAccuracy')),
-      ui.card(t('cards.sessions'), data.sessions.active.toString(), t('meta.activeCount')),
-      ui.card(t('cards.peakActivity'), isPeak ? 'YES' : 'NO', t('meta.highActivity'), isPeak ? 'warning' : 'success')
+      ui.card(t('cards.liveStatus'), '<span style="color:#37b8a8">● LIVE</span>', t('meta.eventStream'), '', 'Real-time event stream status'),
+      ui.card(t('cards.tokens'), data.tokens.used.toLocaleString(), t('meta.currentUsage'), '', 'Current token usage count'),
+      ui.card(t('cards.trafficLight'), `<span style="color:${statusColor}">${data.health.status}</span>`, t('meta.executiveStatus'), '', 'Live system health status'),
+      ui.card(t('cards.routing'), data.health.routing, t('meta.dispatchAccuracy'), '', 'Live routing accuracy'),
+      ui.card(t('cards.sessions'), data.sessions.active.toString(), t('meta.activeCount'), '', 'Active sessions count'),
+      ui.card(t('cards.peakActivity'), isPeak ? 'YES' : 'NO', t('meta.highActivity'), isPeak ? 'warning' : 'success', 'Peak activity hours indicator')
     ]);
     
     // SLA
     ui.renderSection('sla', [
-      ui.card(t('cards.uptime'), '99.9%', t('meta.target') + ': 99.5%', 'success'),
-      ui.card(t('cards.uptime'), '100%', t('meta.target') + ': 99.9%', 'success'),
-      ui.card(t('cards.incidents'), '0', '30d: 0 · 7d: 0', 'success'),
-      ui.card(t('cards.mttr'), '0m', t('meta.meanRecovery')),
-      ui.card(t('cards.routingAcc'), '100%', t('meta.target') + ': 95%', 'success'),
-      ui.card(t('cards.latency'), '1.3s', t('meta.target') + ': 1.5s', 'success')
+      ui.card(t('cards.uptime'), '99.9%', t('meta.target') + ': 99.5%', 'success', 'Monthly uptime percentage. Target: 99.5%'),
+      ui.card(t('cards.uptime'), '100%', t('meta.target') + ': 99.9%', 'success', 'Weekly uptime percentage. Target: 99.9%'),
+      ui.card(t('cards.incidents'), '0', '30d: 0 · 7d: 0', 'success', 'Service incidents in last 30 and 7 days'),
+      ui.card(t('cards.mttr'), '0m', t('meta.meanRecovery'), '', 'Mean Time To Recovery in minutes'),
+      ui.card(t('cards.routingAcc'), '100%', t('meta.target') + ': 95%', 'success', 'Routing accuracy percentage. Target: 95%'),
+      ui.card(t('cards.latency'), '1.3s', t('meta.target') + ': 1.5s', 'success', 'Average response latency. Target: <1.5s')
     ]);
     
     // Performance
     ui.renderSection('perf', [
-      ui.card(t('cards.sessions'), '111', t('meta.last30Days')),
-      ui.card(t('cards.peakActivity'), '17:00', '13 sessions'),
-      ui.card('Sessions/Day', '3.7', t('meta.average')),
-      ui.card('Avg Session', '7 min', 'duration'),
-      ui.card('Active Now', '100', t('meta.fromAnalytics')),
-      ui.card(t('cards.velocity'), '+9%', '60 vs 55', 'success')
+      ui.card(t('cards.sessions'), '111', t('meta.last30Days'), '', 'Total sessions in last 30 days'),
+      ui.card(t('cards.peakActivity'), '17:00', '13 sessions', '', 'Peak activity hour with session count'),
+      ui.card('Sessions/Day', '3.7', t('meta.average'), '', 'Average sessions per day'),
+      ui.card('Avg Session', '7 min', 'duration', '', 'Average session duration'),
+      ui.card('Active Now', '100', t('meta.fromAnalytics'), '', 'Active sessions from analytics'),
+      ui.card(t('cards.velocity'), '+9%', '60 vs 55', 'success', 'Development velocity improvement percentage')
     ]);
