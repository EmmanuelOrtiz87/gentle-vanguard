# Presentations

This directory keeps the current stack presentation.

Older presentation versions should be removed from live docs or moved to `.archive/` only when there
is a specific historical reason to retain them.
