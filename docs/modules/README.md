# Module Documentation

Per-module documentation referenced by the `documentation` gate in
[`config/module-maturity.json`](../../config/module-maturity.json). One file per module:
`<module-id>.md`. Currently optional (non-required gate); recommended before promotion to core.
