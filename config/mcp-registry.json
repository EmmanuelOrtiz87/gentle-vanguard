{
  "version": "2.0.0",
  "description": "MCP Native registry — managed by mcp-manager.ts (TypeScript)",
  "servers": [
    {
      "name": "skill-server",
      "type": "builtin",
      "transport": "stdio",
      "command": "node",
      "args": ["dist/scripts/mcp/skill-server.js"],
      "enabled": true,
      "autoStart": true,
      "description": "143+ internal skills via MCP: list_skills, get_skill, search_skills"
    },
    {
      "name": "engram-mcp",
      "type": "builtin",
      "transport": "stdio",
      "command": "npx",
      "args": ["engram", "mcp"],
      "enabled": true,
      "autoStart": true,
      "description": "Persistent memory via Engram MCP"
    },
    {
      "name": "lsp-server",
      "type": "builtin",
      "transport": "stdio",
      "command": "npx",
      "args": ["tsx", "src/mcp/mcp-lsp-server.ts"],
      "enabled": true,
      "autoStart": true,
      "description": "TypeScript Language Service via MCP: go_to_definition, find_references, get_hover_info, get_completions, get_diagnostics, get_symbol_info, get_file_symbols"
    },
    {
      "name": "fetch-server",
      "type": "builtin",
      "transport": "stdio",
      "command": "npx",
      "args": ["tsx", "src/mcp/fetch-server-native.ts"],
      "enabled": true,
      "autoStart": false,
      "description": "Native web fetch and search for LLMs: fetch_url (Jina Reader), search_web (DDG/Bing)"
    },
    {
      "name": "sequential-thinking",
      "type": "builtin",
      "transport": "stdio",
      "command": "npx",
      "args": ["tsx", "src/mcp/sequential-thinking-server.ts"],
      "enabled": true,
      "autoStart": false,
      "description": "Sequential thinking chains: think_sequential, get_thought_chain, list_thought_chains, get_thought_summary"
    },
    {
      "name": "gv-analytics-atlassian",
      "type": "user",
      "transport": "stdio",
      "command": "node",
      "args": ["--import", "tsx", "apps/gv-analytics/server/mcp.ts"],
      "enabled": true,
      "autoStart": false,
      "description": "Gentle-Vanguard Analytics Atlassian MCP: connection status and delivery analysis"
    }
  ]
}
