Show the current workspace status.

1. `git status` — working tree state
2. `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/utilities/agent-verify.ps1` — stack health
3. Summarize: branch, clean/dirty, tests pass count, warnings
