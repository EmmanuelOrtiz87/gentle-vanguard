{
  "version": "1.0.0",
  "description": "Artifact retention policy for Gentle-Vanguard v2.1+",
  "defaultMaxRepo": 1,
  "defaultMaxLocal": 14,
  "categories": {
    "audits": {
      "maxRepo": 3,
      "maxLocal": 14,
      "description": "Keep last 3 audit reports in repo for trend visibility with lower storage cost"
    },
    "sessions": {
      "maxRepo": 1,
      "maxLocal": 14,
      "description": "Keep only latest session-start file in repo, archive rest"
    },
    "code-reviews": {
      "maxRepo": 1,
      "maxLocal": 14,
      "description": "Keep only latest code review in repo"
    }
  },
  "automation": {
    "triggerOn": ["end-session", "day-end-closure"],
    "manualCommand": ".\\scripts\\utilities\\rotate-artifacts.ps1",
    "ciIntegration": true
  }
}
