{
  "version": "1.0.0",
  "description": "Pre-built MCP server templates for mcp-manager quickstart",
  "templates": [
    {
      "name": "sqlite",
      "description": "Local SQLite database — query tables, run SQL via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sqlite", "--db", "{path}"],
      "defaultPath": ".session/data/mcp.db",
      "transport": "stdio",
      "autoStart": true
    },
    {
      "name": "filesystem",
      "description": "File system access — read, write, search files via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem@2026.7.10", "{path}"],
      "defaultPath": ".",
      "transport": "stdio",
      "autoStart": true
    },
    {
      "name": "memory",
      "description": "Persistent knowledge graph — entities, relations via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory@2026.7.4"],
      "defaultPath": null,
      "transport": "stdio",
      "autoStart": true
    },
    {
      "name": "browser",
      "description": "Headless browser automation — navigate, screenshot, extract via MCP",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@0.0.79"],
      "defaultPath": null,
      "transport": "stdio",
      "autoStart": false
    },
    {
      "name": "git",
      "description": "Git operations — log, diff, status, blame via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-git", "--repository", "{path}"],
      "defaultPath": ".",
      "transport": "stdio",
      "autoStart": false
    },
    {
      "name": "redis",
      "description": "Redis in-memory data store — keys, get, set, query via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-redis@2025.4.25"],
      "defaultPath": null,
      "transport": "stdio",
      "autoStart": false
    },
    {
      "name": "postgres",
      "description": "PostgreSQL database — query tables, run SQL via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres@0.6.2", "{path}"],
      "defaultPath": "postgresql://localhost:5432/mcp",
      "transport": "stdio",
      "autoStart": false
    },
    {
      "name": "puppeteer",
      "description": "Headless Chrome — navigate, screenshot, PDF, extract via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer@2025.5.12"],
      "defaultPath": null,
      "transport": "stdio",
      "autoStart": false
    }
  ]
}
