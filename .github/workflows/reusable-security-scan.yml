name: Reusable Security Scan

on:
  workflow_call:

permissions:
  contents: read

jobs:
  probe:
    name: Parser Probe
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo ok
