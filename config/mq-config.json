{
  "_comment": "MQ adapter config for Gentle-Vanguard event bus. adapter: file | redis | webhook",
  "adapter": "file",
  "redis": {
    "host": "127.0.0.1",
    "port": 6379,
    "channel": "gv-events",
    "_note": "Requires redis-cli in PATH. Set adapter=redis to activate."
  },
  "webhook": {
    "url": "",
    "secret_env": "GF_WEBHOOK_SECRET",
    "_note": "Set url and adapter=webhook. Secret loaded from env var GF_WEBHOOK_SECRET."
  }
}
